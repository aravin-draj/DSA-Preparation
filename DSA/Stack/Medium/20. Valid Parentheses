# 🧮 20. Valid Parentheses

🔗 https://leetcode.com/problems/valid-parentheses/description/

## ✅ Approach:
when a open bracket appears pushing the expected closing braket to compare later,
any other expect a opening bracket should match the top 

## ⏱️ Time Complexity:
No1:
- O(N) time, O(N) space

## 🔤 Code (Java):
```java
class Solution {
    public boolean isValid(String s) {
        Stack<Character> st=new Stack<>();

        for(int i=0;i<s.length();i++){
            char c=s.charAt(i);
            if(c=='('){
                st.push(')');
            }
            else if(c=='{'){
                st.push('}');
            }
            else if(c=='['){
                st.push(']');
            }
            else if(st.isEmpty() || st.pop()!=c){
                return false;
            }
        }
        return st.isEmpty();
    }
}
  
}
